<% if notice %>
<div class="alert alert-info" role="alert">
<p id="notice"><%= notice %></p>
</div>
<% end %>

  <div class="form-group">
  <span><strong>Content</strong></span>
  <span class="form-control" ><%= raw@lecture.content %></span>
</div>

  <div class="form-group">
  <span><strong>Attachment</strong></span>
  <span class="form-control" ><%= @lecture.try(:attachment) %></span>
</div>

  <div class="form-group">
  <span><strong>Course</strong></span>
  <span class="form-control" ><%= @lecture.try(:course).try(:title) %></span>
</div>

  <div class="form-group">
  <span><strong>User</strong></span>
  <span class="form-control" ><%= @lecture.try(:user).try(:name) %></span>
</div>
<hr>
<%= render 'comments/comment' %>

<% if @lecture.user==current_user  %>
<%= link_to 'Edit', edit_lecture_path(@lecture) %> |
<% end %>
<%= link_to 'Back', lectures_path %>
